import { CorectPhoneDirective } from './corect-phone.directive';

describe('CorectPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new CorectPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
